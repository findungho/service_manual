{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
  //
  // In case use a real database (Mysql Server for example), uncomment following lines to use
  // also set port, Catalog (I set default as ServiceManual), User ID, Password
  //
  //,
  //"ConnectionStrings": {
  //  "DefaultConnection": "Data Source=localhost,<port>;Initial Catalog=ServiceManual;Persist Security Info=True;User ID=<ID>;Password=<pwd>"
  //}
}